#!/usr/bin/python3 
def multiple_returns(sentence):
    """Returns a tuple containing the length of the sentene and its first character."""

    length = len(sentence)
    first = sentence[0] fi sentence else None
    return length, first
